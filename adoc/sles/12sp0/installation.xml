<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd"[
<!ENTITY % myents SYSTEM "release-notes.ent" >
%myents;
]>

<chapter id="InstUpgrade">
 <title>Installation and Upgrade</title>
 <!-- <para /> -->

 <!-- <section id="Installation.Deployment"> -->
 <!--  <title>Deployment</title>  -->
  <remark>emap: hope the ids I make up are okay.</remark>
  <remark>ke: In the long run, we probably must add them to fate, too.  jw will work it out eventually :)</remark>
  <para>&sles; can be deployed in three ways:</para>
  <itemizedlist>
   <listitem>
    <para>Physical Machine,</para>
   </listitem>
   <listitem>
    <para>Virtual Host,</para>
   </listitem>
   <listitem>
    <para>Virtual Machine in paravirtualized environments.</para>
   </listitem>
  </itemizedlist>
 <!-- </section> -->

 <section id="InstUpgrade.Installation">
  <title>Installation</title>

<!-- now via fate#317705
 <section id="Installation.CJK">
  <title>CJK Languages Support in Text-mode Installation</title>
-->

 <section id="Installation.BootGrub2TB">
  <!-- bnc#719429 -->
  <title>Booting from Harddisks larger than 2 TiB in Non-UEFI Mode</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   Booting from harddisks larger than 2 TiB in non-UEFI mode (but with
   GPT partition table) fails.
  </para>

  <para>
   To successfully use harddisks larger than 2 TiB in non-UEFI mode, but
   with GPT partition table (i.e., grub bootloader), consider one of the
   following options:
  </para>

  <itemizedlist>
   <listitem>
    <para>
     Use a 4k sector harddisk in 4k mode (in this case, the 2 TiB limit will
     become a 16 TiB limit).
    </para>
   </listitem>
   <listitem>
    <para>
     Use a separate <filename>/boot</filename> partition. This partition
     must be one of the first 3 partitions and end below the 2 TiB
     limit.
    </para>
   </listitem>
   <listitem>
    <para>
     Switch from legacy mode to UEFI mode, if this is an option for you.
    </para>
   </listitem>
  </itemizedlist>
 </section>

 <!-- 
 <section id="Installation.PerstDevNames">
  <title>Installation Using Persistent Device Names</title>
  <para>CHECKIT:SLE11.3</para>
    <para>
     The installer uses persistent device names by default. If you plan
     to add storage devices to your system after the installation, we
     strongly recommend you use persistent device names for all storage
     devices.
    </para>
    <!-\- bnc#732999 -\->
    <para>
     To switch to persistent device names on a system that has already
     been installed, start the YaST2 partitioner. For each partition,
     select <guimenu>Edit</guimenu> and go to the <guimenu>Fstab
     Options</guimenu> dialog. Any mount option except <guimenu>Device
     name</guimenu> provides you persistent device names. In addition,
     rerun the Boot Loader module in YaST and select <guimenu>Propose
     New Config</guimenu> to switch the boot loader to using the
     persistent device name, or manually adjust all boot loader
     sections. Then select <guimenu>Finish</guimenu> to write the new
     proposed configuration to disk.  Alternatively, edit
     <filename>/boot/grub/menu.lst</filename> and
     <filename>/boot/grub/device.map</filename> according to your needs.
    </para>
    <para>
     This needs to be done before adding new storage devices.
    </para>
     <!-\- Old entry:
     To switch to persistent device names on a system that has already
     been installed, start the YaST2 partitioner. For each partition,
     select <guimenu>Edit</guimenu> and go to the <guimenu>Fstab
     Options</guimenu> dialog. Any mount option except <command>Device
     name</command> provides you persistent device names. In addition,
     rerun the boot loader module in YaST to switch the bootloader to
     using the persistent device name. Start the module <guimenu>Boot
     Loader</guimenu> and select <guilabel>Finish</guilabel> to write
     the new proposed configuration to disk. This needs to be done
     before adding new storage devices.
     -\->
    <!-\- Do not refer opensuse.org -\->
    <para>
      For further information, see the <quote>Storage Administration
      Guide</quote> about "Device Name Persistence".
    </para>
 </section>
 -->

 <!-- bnc#746579 -->
 <section>
  <title>iSCSI Booting with iBFT in UEFI Mode</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
If booting over iSCSI, iBFT information cannot be parsed when booting via
native UEFI.  The system should be configured to boot in legacy mode if iSCSI
booting using iBFT is required.
  </para>
 </section>

 <!-- relnotes.i.withiscsi -->
 <!-- bnc#818162; SLES 11 SP3 -->
 <!-- bnc#882257: removing
 <section id="Installation.iSCSI">
  <title>Using iSCSI Disks when Installing</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   To use iSCSI disks during installation, passing the
   <literal>withiscsi</literal> boot parameter is no longer needed.
  </para>
  <para>
   During installation, an additional screen provides the option to
   attach iSCSI disks to the system and use them in the installation
   process.
  </para>
  <para>
   Booting from an iSCSI server on i386, x86_64 and ppc64 is supported
   if iSCSI-enabled firmware is used.
  </para>
  <!-\- bnc#594957 mge@novell.com 20100508
                   2011-11-08 ke: remove now superfluous note -\->
 </section>
 -->
 <!-- relnotes.i.qla_xxx -->
 <!-- bnc#745201 -->
 <!-- 2013-12-03
 <section id="Installation.qla">
  <title>Using qla3xxx and qla4xxx Drivers at the Same Time</title>
  <para>
   QLogic iSCSI Expansion Card for IBM BladeCenter provides both
   Ethernet and iSCSI functions. Some parts on the card are shared by
   both functions. The current qla3xxx (Ethernet) and qla4xxx (iSCSI)
   drivers support Ethernet and iSCSI function individually. In contrast
   to previous SLES releases, using both functions at the same time is
   now supported.
  </para>
  <para>
   If you happen to use <literal>brokenmodules=qla3xxx</literal> or
   <literal>brokenmodules=qla4xxx</literal> before upgrading to SLES 11 SP2,
   these options can be removed.
  </para>
 </section>
 -->

 <!-- relnotes.i.use_edd -->
 <!--
 <section id="Installation.EDD">
  <title>Using EDD Information for Storage Device Identification</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   EDD information (in
   <filename>/sys/firmware/edd/&lt;device&gt;</filename>) is used by
   default to identify your storage devices.
  </para>
  <para>EDD Requirements:</para>
  <itemizedlist>
   <listitem>
    <para>
     BIOS provides full EDD information (found in
     <filename>/sys/firmware/edd/&lt;device&gt;</filename>)
    </para>
   </listitem>
   <listitem>
    <para>
     Disks are signed with a unique MBR signature (found in
     <filename>/sys/firmware/edd/&lt;device&gt;/mbr_signature</filename>).
    </para>
   </listitem>
  </itemizedlist>
  <para>
   Add <literal>edd=off</literal> to the kernel parameters to disable
   EDD.
  </para>
 </section>
 -->

 <!-- relnotes.i.autoinstall -->
 <section id="Installation.autoyast">
  <title>System z: Automatic Installation with &autoyast; in an LPAR</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   For automatic installation with &autoyast; in an LPAR, the
   <filename>parmfile</filename> used for such an installation must have
   blank characters at the beginning and at the end of each line (the
   first line does not need to start with a blank). The number of
   characters in one line should not exceed 80.
  </para>
 </section>

 <!-- relnotes.i.dasd -->
 <section id="Installation.Systemz">
  <title>System z: Adding DASD or zFCP Disks During Installation</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   Adding of DASD or zFCP disks is not only possible during the
   installation workflow, but also when the installation proposal is
   shown. To add disks at this stage, click on the
   <guimenu>Expert</guimenu> tab and scroll down. There the DASD and/or
   zFCP entry is shown. These added disks are not displayed in the
   partitioner automatically. To make the disks visible in the
   partitioner, you have to click on <guimenu>Expert</guimenu> and
   select <guimenu>reread partition table</guimenu>. This may reset any
   previously entered information.
  </para>
 </section>

 <!-- bnc#448547 -->
 <section id="Installation.Network">
  <title>POWER: Network Installation via eHEA</title>
  <para>CHECKIT:SLE11.3</para>  <para>
   If you want to carry out a network installation via the IBM eHEA
   Ethernet Adapter on POWER systems, no huge (16GB) pages may be
   assigned to the partition during installation.
  </para>
 </section>

</section>
<!--
 <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
             href="update.xml"/>
-->
 <section id='InstUpgrade.Upgrade'>
  <title>Upgrade-Related Notes</title>
  <para>
   This section includes upgrade-related information for this release.
  </para>
 </section>
<section>
  <title>For More Information</title>
  <para>For more information, see <xref
  linkend="InfraPackArch"/>.</para>
 </section>
</chapter>
