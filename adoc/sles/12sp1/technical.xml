<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd"[
<!ENTITY % myents SYSTEM "release-notes.ent" >
%myents;
]>

<!-- -=-=-=-=-=-=-=-=-=-=-=-=-=- cut here -=-=-=-=-=-=-=-=-=-=-=-=-=- -->
<!--
     NOTE: outdated stuff.
           if still needed move to fate.
-->
<!-- -=-=-=-=-=-=-=-=-=-=-=-=-=- cut here -=-=-=-=-=-=-=-=-=-=-=-=-=- -->


<chapter id="TechInfo.Other">
 <title>Other Technical Information</title>

 <!-- This section includes technical information -->

 <!-- bnc#598405 -->
 <!-- bnc#740474 -->
 <!-- updated by ihno for SP2 -->
 <!-- updated via mcowly for SP3 -->
 <section>
  <title>libica 2.1.0 Available since SLES 11 SP2 for s390x</title>

  <para>CHECKIT:SLE11.3</para>
   <para>
    The libica package contains the interface library routines used by
    IBM modules to interface with IBM Cryptographic Hardware
    (ICA). Starting with SLES 11 SP1, libica is provided in the s390x
    distribution in three flavors of packages: libica-1_3_9,
    libica-2_0_2, and libica-2_1_0 providing libica versions 1.3.9,
    2.0.2, and 2.1.0 respectively.
   </para>
   <para>
    libica 1.3.9 is provided for compatibility reasons with legacy
    hardware present e.g. in the ppc64 architecture. For s390x users it
    is always recommended to use the new libica 2.1.0 library since it
    supports all newer s390x hardware, larger key sizes and is backwards
    compatible with any ICA device driver in the s390x architecture.
   </para>
   <para>
    You may choose to continue using libica 1.3.9 or 2.0.2 if you do not
    have newer Cryptographic hardware to exploit or wish continue using
    custom applications that do not support the libica 2.1.0 library
    yet. Both openCryptoki and openssl-ibmca, the two main exploiters
    for the libica interface, are provided starting with SLES 11 SP2 to
    support the newer libica 2.1.0 library.
   </para>
 </section>

 <section>
  <!-- bnc#728611 -->
  <title>&yast; Support for Layer 2 Devices</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
   &yast; writes the MAC address for layer 2 devices only if they are of
   the card_types:
  </para>
  <itemizedlist>
    <listitem>
     <para>OSD_100</para>
    </listitem>
    <listitem>
     <para>OSD_1000</para>
    </listitem>
    <listitem>
     <para>OSD_10GIG</para>
    </listitem>
    <listitem>
     <para>OSD_FE_LANE</para>
    </listitem>
    <listitem>
     <para>OSD_GbE_LANE</para>
    </listitem>
    <listitem>
     <para>OSD_Express</para>
    </listitem>
  </itemizedlist>
  <para>
   Per intent &yast; does not write the MAC address for devices of the types:
  </para>
  <itemizedlist>
    <listitem>
     <para>HiperSockets</para>
    </listitem>
    <listitem>
     <para>GuestLAN/VSWITCH QDIO</para>
    </listitem>
    <listitem>
     <para>OSM</para>
    </listitem>
    <listitem>
     <para>OSX</para>
    </listitem>
  </itemizedlist>
 </section>
 <!-- 2013-12-03
 <section>
  <!-\- bnc#440153 -\->
  <title>Changes to Network Setup</title>
    <para>
     The script <command>modify_resolvconf</command> is removed in favor
     of a more versatile script called
     <command>netconfig</command>. This new script handles specific
     network settings from multiple sources more flexibly and
     transparently. See the documentation and man-page of
     netconfig for more information.
    </para>
 </section>
 -->
 <!-- requested by Jiri Slaby jslaby@novell.com -->
 <section>
  <title>Memory cgroups</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
    Memory cgroups are now disabled for machines where they cause memory
    exhaustion and crashes. Namely, X86 32-bit systems with PAE support
    and more than 8G in any memory node have this feature disabled.
  </para>
 </section>

 <!-- requested by Thomas Renninger trenn@novell.com -->
 <!-- 2013-12-03, ke: adjusted for systemd -->
 <!-- bnc#853371 : moving to the books? -->
 <section>
  <title>MCELog</title>
   <para>
    The mcelog package logs and parses/translates Machine Check
    Exceptions (MCE) on hardware errors (also including memory
    errors). Formerly this has been done by a cron job executed
    hourly. Now hardware errors are immediately processed by an mcelog
    daemon.
   </para>
   <para>
    However, the mcelog service is not enabled by default resulting in
    memory and CPU errors also not being logged by default. In addition,
    mcelog has a new feature to also handle predictive bad page
    offlining and automatic core offlining when cache errors happen.
   </para>
   <para>
    The service can either be enabled via the YaST system services
    editor or via command line with:
    </para>
    <screen>systemctl enable mcelog.service
systemctl start mcelog.service</screen>
 </section>

 <!-- relnotes.t.i18n -->
 <section>
   <title>Locale Settings in <filename>~/.i18n</filename></title>
   <para>CHECKIT:SLE11.3</para>
   <para>
    If you are not satisfied with locale system defaults, change the
    settings in <filename>~/.i18n</filename>. Entries in
    <filename>~/.i18n</filename> override system defaults from
    <filename>/etc/sysconfig/language</filename>.  Use the same variable
    names but without the <literal>RC_</literal> namespace prefixes; for
    example, use <literal>LANG</literal> instead of
    <literal>RC_LANG</literal>. For more information about locales in
    general, see "Language and Country-Specific Settings" in the
    Administration Guide.<!-- on sle, it is called admin guide -->
   </para>
 </section>

 <!-- relnotes.g.kdump -->
 <section>
  <title>Configuration of kdump</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
    kdump is useful, if the kernel is crashing or otherwise misbehaving
    and a kernel core dump needs to be captured for analysis.
   </para>
   <para>
    Use YaST (<menuchoice><guimenu>System</guimenu><guimenu>Kernel
    Kdump</guimenu></menuchoice>) to configure your environment.
   </para>
 </section>

 <!-- bnc#706059 -->
 <!-- bnc#818576 -->
 <!-- 2013-12-03, ke: adjusted for systemd -->
 <section>
  <title>Configuring Authentication for kdump through YaST with ssh/scp as
   Target</title>
  <para>CHECKIT:SLE11.3</para>
  <para>
    When kdump is configured through YaST with ssh/scp as target and the
    target system is SUSE Linux Enterprise, then enable authentication
    using either of the following ways:
  </para>
   <orderedlist>
    <listitem>
     <para>
      Copy the public keys to the target system:
     </para>

     <screen>ssh-copy-id -i ~/.ssh/id_*.pub  &lt;username>@&lt;target system IP></screen>
     <para>
      or
     </para>
    </listitem>
    <listitem>
     <para>
      Change the <literal>PasswordAuthentication</literal> setting in
      <filename>/etc/ssh/sshd_config</filename> of the target system
      from:
     </para>

     <screen>PasswordAuthentication no</screen>

     <para>
      to:
     </para>

     <screen>PasswordAuthentication yes</screen>
    </listitem>

    <listitem>
     <para>
      After changing <literal>PasswordAuthentication</literal> in
      <filename>/etc/ssh/sshd_config</filename> restart the sshd service
      on the target system with:
     </para>

     <screen>systemctl restart sshd.service</screen>
    </listitem>

   </orderedlist>
 </section>


 <!-- relnotes.t.jpackage -->
 <!-- 2013-12-03
 <section>
   <title>JPackage Standard for Java Packages</title>
   <para>
    Java packages are changed to follow the JPackage Standard (<ulink
    url="http://www.jpackage.org/"/>). For more information, see the
    documentation in
    <filename>/usr/share/doc/packages/jpackage-utils/</filename>.
   </para>
 </section>
 -->


 <!-- bnc#622203 -->
 <section>
   <title>Stopping Cron Status Messages</title>
   <para>CHECKIT:SLE11.3 bnc#853373</para>
   <para>
To avoid the mail-flood caused by cron status messages, the default
value of <literal>SEND_MAIL_ON_NO_ERROR</literal> in
<filename>/etc/sysconfig/cron</filename> is set to
"<literal>no</literal>" for new installations.  Even with this setting
to "<literal>no</literal>", cron data output will still be send to the
<literal>MAILTO</literal> address, as documented in the cron manpage.
   </para>
   <para>
In the update case it is recommended to set these values according to
your needs.
   </para>
 </section>
</chapter>
